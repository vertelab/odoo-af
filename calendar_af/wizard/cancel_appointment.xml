<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="cancel_appointment_view_form" model="ir.ui.view">
        <field name="name">calendar_cancel_appointment.form</field>
        <field name="model">calendar.cancel_appointment</field>
        <field name="arch" type="xml">
            <form string="Cancel appointment">
                <group>
                    <field name="cancel_reason" required="1"/>
                    <field name="appointment_ids" readonly="1" string="">
                        <tree>
                            <field name="type_id"/>
                            <field name="start"/>
                            <field name="duration_text"/>
                            <field name="user_id"/>
                            <field name="partner_id"/>
                            <field name="operation_id"/>
                        </tree>
                        <form>
                        </form>
                    </field>
                </group>
                <footer>
                    <group>
                        <span>
                            <button string="OK" type="object" class="oe_highlight" name="action_cancel_appointment"/>
                            <button string="Cancel" class="oe_link" special="cancel"/>
                        </span>
                    </group>
                </footer>
            </form>
        </field>
    </record>

    <record id="cancel_appointment_form_action" model="ir.actions.act_window">
        <field name="name">Cancel appointment</field>
        <field name="res_model">calendar.cancel_appointment</field>
        <field name="view_type">form</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <act_window id="cancel_appointment_action"
                name="Cancel appointment"
                src_model="calendar.appointment"
                res_model="calendar.cancel_appointment"
                view_type="form"
                view_mode="form"
                key2="client_action_multi"
                target="new"/>

</odoo>
